# 07 — MODEL CALL LOG — Registro de chamadas reais a modelos externos

> Regra: não simular chamadas. Toda chamada real é registrada aqui (canal, modelo, propósito, resultado). Limite: máximo 5 chamadas externas por ciclo sem autorização.

| # | Data | Canal | Modelo | Propósito | Resultado |
|---|---|---|---|---|---|
| 1 | 2026-07-10 | OpenAI API (`/v1/chat/completions`, key do `~/.zshrc`) | gpt-5.2 (tentado) | Teste de integração da Sessão 0 | **FALHA** — `invalid_api_key` (sk-proj-…t1QA rejeitada). Estado: AGUARDANDO INTEGRAÇÃO (renovar key). `/v1/models` também retornou vazio. |
| 2 | 2026-07-10 | Codex MCP (auth ChatGPT, sandbox read-only) | "Codex, an OpenAI agent based on GPT-5" (auto-identificação) | Mesmo teste, canal alternativo | **SUCESSO** — confirmou: (1) identidade GPT-5/Codex; (2) mensagem recebida standalone, sem contexto desta conversa; (3) YES para atuar como referee adversarial de claim packages. threadId 019f4e90-cb71-7da0-92b0-e37be28b4e20. |
| 3 | 2026-07-10 | Codex MCP (read-only) | Codex/GPT-5 | **REV-0001** — primeira revisão adversarial real: claim package 7P-PNP-CLM-0010 (lema 3COL→SAT), protocolo oficial completo | **SUCESSO — GAP_FOUND na parte (b):** lógica (a) validada nas duas direções (incl. confirmação da redundância do grupo (2)); contagem 4n+3\|E\| confirmada; alegação de tempo O(n+\|E\|) corretamente contestada — depende de representação (lista de arestas vs matriz Θ(n²)) e modelo (word-RAM vs bits: Θ((n+\|E\|)log n)). Contraexemplo concreto (grafo vazio em matriz de adjacência). Hipóteses ausentes apontadas: representação, codificação, modelo, convenção n=0. Correção mínima proposta e ACEITA. Confiança: alta. threadId 019f4ec6-2429-79d3-b6b1-1ef528365c14. |

## Pacote que teria sido enviado pela API direta (registrado conforme protocolo de falha)

```
Integration test for a mathematical review pipeline. Reply with exactly:
(1) your model identity, (2) confirmation you received this as a standalone
message with no prior conversation context, (3) YES/NO whether you could
analyze a structured mathematical claim package (statement, hypotheses,
proof sketch, complexity claims). Max 60 words.
```

| 4 | 2026-07-10 | Kimi plugin (kimi-review, read-only, agente ad1d5de7ac95ad3ac) | Kimi (Moonshot) | **REV-0002** — revisão adversarial do DIFF COMPLETO do branch (10 commits, 33+ arquivos) antes do merge do PR #6 | **SUCESSO — veredito "concern", 12 findings** (6 importantes + 6 menores). Adjudicação completa no RESEARCH_LOG (Ciclo 6): 10 aceitos e corrigidos; 1 parcialmente aceito (finding 4 alegou inversão do Thm 4.1 de RR — o verbatim mostra que era AMBIGUIDADE, não inversão; redação precisada); 1 aceito com interpretação (finding 6 — registro completo instituído para claims derivados). Gerou 2 regras novas: convenção de Estado com lista fechada + regra de dupla família para dependências. Nota operacional: wrapper exigiu `CLAUDE_PLUGIN_DATA` manual. |

| 5 | 2026-07-10 | Grok wrapper (grok-adversary, read-only, agente a280c8bae549893ff) | Grok 4.5 (xAI) | **REV-0003** — challenge adversarial da proposta de seleção da FASE 5 (C1 + unidade "2 classes NPN-4") | **SUCESSO — veredito DERRUBAR, 11 findings.** Principais: ponderação executabilidade>relevância; INT inflado; dependência de preprint único sem confirmar gap vivo; capacidade computacional irreal (timeout do próprio autor; sem stack cube-and-conquer); auditoria assimétrica (C3/C4 sem profundidade); valor científico modesto. Recomendou C2 ou C4; se C1, reproduzir classe já resolvida com DRAT + critérios de aborto. Adjudicação: 4 aceitos, 2 aceitos em parte → proposta v3 (piloto reprodutivo + auditoria C3/C4 + seleção definitiva depois). Registro em `PNP_AI/09_CANDIDATE_SUBPROBLEMS.md`. |

| 6 | 2026-07-11 | Codex MCP (read-only) | Solicitado: GPT-5.6 (Luiz). **Canal rejeitou** `gpt-5.6` e `gpt-5.6-codex` (não suportados no plano ChatGPT). Usado: default do canal (auto-id: "Codex, baseado em GPT-5, checkpoint não especificado") | **REV-0004** — revisão de confirmação da proposta v4 de seleção (FASE 5) | **SUCESSO — veredito DERRUBAR, 11 findings.** Centrais: seleção fechada sem executar o gate que a v3 definia; REL 3→4 não demonstrado (relação temática ≠ avanço); dupla adaptação pós-hoc do scorecard; **DRAT certifica a CNF, não o encoding** (exige validação semântica independente — virou regra); Unidade 1 só vale como gate se puder reprovar C1; "gap confirmado vivo" = overclaim; C2 sem alvo na mesma granularidade; n=5 = promessa sem benchmark. Adjudicação: TODOS aceitos → proposta v5 (seleção PENDENTE + gate pré-registrado). threadId 019f5081-9923-7071-8f0c-f53e1ed8dd9a. |

| 7 | 2026-07-11 | Codex CLI (`codex exec`, teste de canal, fora de ciclo de revisão) | gpt-5.6-sol | **Correção da nota de canal da REV-0004** — verificar se GPT-5.6 existe no plano ChatGPT sob outro model id | **SUCESSO — o id correto é `gpt-5.6-sol`** (é o default do `~/.codex/config.toml`, reasoning high). Confirmado também que `gpt-5.6` e `gpt-5.6-codex` retornam 400 (`not supported when using Codex with a ChatGPT account`). A conclusão "GPT-5.6 não disponível no plano" fica **CORRIGIDA**: disponível via `gpt-5.6-sol`. Futuras chamadas: omitir o model id (default já é o certo) ou passar `gpt-5.6-sol` explícito. A REV-0004 permanece válida como executada (GPT-5-based default). |

| 8 | 2026-07-11 | GLM wrapper (glm-adversary, read-only, agente ade22d2b60528484f) | GLM-5.2 (Zhipu) | **REV-0005** — auditoria adversarial do re-check de novidade do resultado 0x1669/0x166b (dossiê com evidência web/GitHub coletada por Claude: arXiv só v1; repo sem commits desde 2026-03-10; zero issues/PRs; CSV público byte-idêntico ao local, SHA-256 `5328e44f…`, 2 classes ainda `improved_ub`; Semantic Scholar citations = lista vazia; buscas diretas sem resultado) | **SUCESSO — veredito SUSTENTADA** (gap continua aberto; resultado novo o bastante para contribuição ao repo). Findings acionáveis: (1) Scholar/Semantic Scholar eram a lacuna de maior impacto — Semantic Scholar fechado no mesmo ciclo (0 citações; metadata 429, citations OK); (2) literatura clássica de exact synthesis (ABC/mockturtle, Haaswijk/Soeken) não varrida — risco residual declarado; (3) **finding real de soundness:** encoder pergunta "exatamente k"; UNSAT k=9 sozinho não fecha k≤8 sem lema de padding/minimalidade → varredura empírica k=1..8 executada no mesmo ciclo (ver 12_EXPERIMENTS addendum); (4) doc stale em 12_EXPERIMENTS (cabeçalho "EM ANDAMENTO") — corrigido; (5) para contato com autor: anexar circuitos+CNF+hashes+versões e perguntar "gap ainda aberto?" ANTES de reivindicar novidade. Primeira chamada GLM do programa — canal validado. |

| 9 | 2026-07-11 | GLM wrapper (glm-adversary, read-only, agente a570dfe26df0e892c) | GLM-5.2 (Zhipu) | **REV-0006** — 2ª revisão de família distinta do claim 7P-PNP-CLM-0010 (lema 3COL→SAT), cumprindo a regra de dupla família antes de liberar o claim como dependência | **SUCESSO — veredito VALIDADO, 0 críticos/importantes, 5 MENOR.** GLM verificou empiricamente a aritmética (C5: 35 cláusulas/75 literais; Petersen: 85/180; K4: 34/72 — todos conferem), as duas direções da prova, e CONFIRMOU a correção da alegação de redundância do grupo (2) para a decisão. Findings adjudicados (todos aceitos): F2 encoding p-razoável explicitado na hipótese word-RAM (o único que tangenciava a polinomialidade); F1 multiarestas agora rejeitadas na implementação; F3 caso n=0 na bateria de testes; F4 contagem de variáveis (3n) explícita; F5 nota de estilo. Correções aplicadas e re-executadas (mesmos resultados). **Claim 0010: dupla família CUMPRIDA (OpenAI + Zhipu) — liberado como dependência.** |

| 10 | 2026-07-11 | Codex MCP (read-only, threadId 019f51d7-05cf-7e73-8d79-b3f078242723) | gpt-5.6-sol (default do canal) | **REV-0007** — revisão adversarial da nota técnica v1 (`PNP_AI/notes/technote_npn4_gap_closure.md`) contra os registros internos, antes de Luiz ver | **SUCESSO — veredito NEEDS_REVISION, 14 findings (6 MAJOR, 8 MINOR), TODOS adjudicados como aceitos → v2.** Centrais: (1-2) encoding é "exatamente k NORMALIZADO" e o lema de minimalidade estava incompleto (faltava tratar fan-ins iguais/a<b e o redirecionamento de fanouts de duplicatas) → lema completo enunciado e provado em esboço na v2; (3) varredura k=1..8 NÃO é via independente do lema e é não-certificada → reclassificada como sanity check (correção datada também no 12_EXPERIMENTS); (4) rerun do verify_all sem artefato → arquivado em `experiments/exp_verify_rerun/` (diff, snapshot do script, saída, hashes); (7) confusão GB/GiB nos registros → bytes exatos registrados; (9) "kissat from source em ambas" era falso p/ o Mac (brew) e commit do drat-trim do pod não registrado → corrigido com o gap de proveniência DECLARADO; (10) "independente" rebaixado a "runs/builds separados em 2 arquiteturas"; (11) determinismo de prova ≠ prova byte-idêntica (as provas das 2 máquinas diferem) → reescrito; restantes: notação auto-contida, hedging de novidade, prosa. Nota: confirmou empiricamente vars/cláusulas k=8/9/10, testemunhas, tempos, hashes e core stats ("most numerical claims check out"). |

| 11 | 2026-07-11 | Kimi companion (review, `--base main`, background da sessão principal; 2 tentativas anteriores falharam — teto de Bash de subagente aos ~11min e CLAUDE_PLUGIN_DATA ausente → motivaram o `tools/rev.sh`) | kimi-for-coding (thinking high) | **REV-0008** — review do branch feat/7problems-fase6: código do piloto n=5 (URGENTE, rodando em hardware pago) + nota técnica v2 | **SUCESSO — veredito "concern", 5 findings (3 MEDIUM, 2 LOW), todos aceitos.** Ciência confirmada (canonicalização NPN, encoding, direção HT ✓). F1/F2 = bugs operacionais REAIS no runner (budget sem contar encode → risco de crash de shard; assert/parse abortando shard) → runner v2 + redeploy em voo com resume; F3 = k=1..8 sem DRAT → `cert_lowk.py` executado; F4 = tempos sem encode → `enc_times`; F5 = HT aproximado sob o desenho → declarado. Alerta lateral do plugin: safety hook com path drift (recomendação a Luiz: `/kimi:setup`). Adjudicação completa no Ciclo 16. |

| 12 | 2026-07-11 | Grok wrapper via `tools/rev.sh grok` (background da sessão principal — 1º uso do lançador novo) | Grok 4.5 (xAI) | **REV-0009** — auditoria adversarial do contraexemplo ao Teorema 2 ("Unit Gap") de SRC-0032: dossiê + paper + script | **SUCESSO — veredito SUSTENTADA.** Confirmou: (1) o paper mistura DUAS grandezas — tree_formula (recursão correta, filhos árvores) vs tree_oneshot (identidade do §2 com opt nos filhos, ≤opt+1 por construção); nenhuma leitura honesta com o abstract salva o Thm 2; (2) blindou o contraexemplo com sanduíche independente do nosso DP: Khrapchenko ⟹ tree(⊕₃)≥8, construção explícita de 9 portas ⟹ ≤9, logo gap∈{2,3}>1 mesmo se o DP tivesse furo; (3) BONUS: derivou refutação INDEPENDENTE de Cor. 6 e Thm 7 — s=3 no circuito ótimo de ⊕₃ (verificamos mecanicamente: identidade 1+4+4−3=6, opt do filho=4 com UNSAT@3 DRAT); (4) Thms 3 e 4 sobrevivem (Thm 3 até fica mais limpo sem o Unit Gap); (5) validou nosso DP item a item (base, polaridades, folhas repetíveis, ponto fixo, equivalência AIG↔De Morgan). Claims 0024/0025 registrados. Verbatim em `PNP_AI/experiments/exp_unitgap_check/rev0009_grok_verbatim.md`. |

| 13 | 2026-07-11 | Codex CLI via `tools/rev.sh codex` (`codex exec`, read-only, background da sessão principal; session 019f524d-25d3) | gpt-5.6-sol (reasoning high) | **REV-0010** — 2ª família (regra de dupla família) sobre os claims 0024/0025, com instrução de re-derivar números e contestar a REV-0009 | **SUCESSO — veredito SUSTENTADA, 9 findings, com RE-DERIVAÇÃO INDEPENDENTE:** enumeração própria por camadas de custo confirmou tree(⊕₃)=9 EXATO (contagens 24,64,30,80,32,0,16,0,2 por custo 1..9; ⊕₃ e ¬⊕₃ são as 2 únicas funções de custo 9); re-executou UNSAT@5 (⊕₃) e UNSAT@3 (filho); auditou o DP item a item (correto); conferiu Khrapchenko (144/16=9 folhas ⟹ ≥8 portas); confirmou s=3 ⟹ Cor. 6 falso e apontou que o "optimal substructure" (Remark 5) também falha com portas compartilhadas. **Refinamentos aceitos:** (F7) Thm 7 fica SEM PROVA e sua alegação universal é falsa, mas o enunciado condicional a gap=1 não é refutado por ⊕₃ sob a definição padrão — claim 0025 PRECISADO com correção datada; (F8) prova publicada do Thm 3 tem furo de contagem (|S|≥k−1 falha p/ porta de entrada) — Codex forneceu prova corrigida (2m≥n+m); (F5) CNF+DRAT permanentes arquivados em `certs/` (regeneração byte-idêntica conferida pelos hashes). **Dupla família CUMPRIDA nos claims 0024/0025.** |

## Canais adversariais disponíveis

Todas as 4 famílias testadas e operacionais: Codex/OpenAI (REV-0001/0004/0007), Kimi/Moonshot (REV-0002/0008), Grok/xAI (REV-0003/0009), GLM/Zhipu (REV-0005/0006); Codex também REV-0010. **Lançamento SEMPRE via `tools/rev.sh` (hard rule no CLAUDE.md do projeto).**
